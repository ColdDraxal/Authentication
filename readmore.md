Django -> Object -> Cannot show the data in object as an api response

{
    "id": 1,
    "name": ":Laptop",
    "price": 12000,
    "description": "Gaming Laptop"
}


To convert the data in such a way we have to use serializer

django -> object -> json -> api response -> frontend